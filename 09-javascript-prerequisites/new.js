function Fruit(color, taste, seeds) {
  this.color = color;
  this.taste = taste;
  this.seeds = seeds;
}

// Create an object
const fruit1 = new Fruit("Yellow", "Sweet", 1);

// Display the result
console.log(fruit1.color);
